{
  "dependencies": {
    "concurrently": "^9.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "scripts": {
  "start": "node backend/server.js",
  "postinstall": "cd frontend && npm install && npm run build && npm rebuild sqlite3 --build-from-source"
}
}